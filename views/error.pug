extends layout

block content
  .row.justify-content-center
    .col-md-8.text-center
      h1= message
      h2= error.status
      if process.env.NODE_ENV === 'development'
        pre.mt-4.bg-light.p-3.rounded #{error.stack}
      
      .mt-4
        a.btn.btn-primary(href='/') Return to Home