extends layout

block content
  .text-center.my-5
    h1.display-1 Error
    h2.mb-4= message
    if error && error.status
      p.lead= error.status
    p.lead Something went wrong. Please try again later.
    a.btn.btn-primary.mt-3(href="/") Go Home